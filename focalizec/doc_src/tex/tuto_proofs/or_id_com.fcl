open "basics" ;;

let id (x) = x ;;     (* Definition of the identity function. *)

theorem or_id_commutative:
  all a b c d : int, (id (a) = b \/ id (c) = d) -> (c = id (d) \/ a = id (b))
proof = conclude ;;
